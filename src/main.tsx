import React from "react";
import ReactDOM from "react-dom/client";
import { Function_components } from "./App.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Function_components />
  </React.StrictMode>
);
